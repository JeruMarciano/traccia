{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The renderer draws the map and can open the system print dialog for the current window. It holds no filesystem, shell, network or file-open/save dialog capability.",
  "windows": ["main"],
  "permissions": ["core:default", "core:webview:allow-print"]
}
